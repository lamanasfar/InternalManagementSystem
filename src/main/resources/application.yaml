spring:
  application:
    name: officeManagementSystem
  datasource:
    url: jdbc:postgresql://localhost:5432/officedb
    username: postgres
    password: lyamanava048
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  liquibase:
    enabled: true
    change-log: classpath:db/changelog-master.yml
server:
  port: 9000
